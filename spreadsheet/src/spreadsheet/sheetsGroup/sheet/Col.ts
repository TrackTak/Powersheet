class Col {
  constructor(
    public number: number,
    public minWidth: number,
    public width: number
  ) {
    this.number = number;
    this.minWidth = minWidth;
    this.width = width;
  }
}

export default Col;
